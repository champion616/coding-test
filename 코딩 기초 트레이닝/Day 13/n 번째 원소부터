class Solution {
    public int[] solution(int[] num_list, int n) {
        int[] answer = new int[num_list.length-n+1];
        
        for(int i=0; i<answer.length; i++){
            answer[i] = num_list[n-1+i];
        }
        
        return answer;
    }
}
/*
1. answer 배열의 길이를 num_list.length - n + 1 로 한다.
2. i = n, answer 배열의 길이만큼 반복한다. 
3. num_list[i-1]의 원소를 answer 배열에 차례대로 담는다.
*/
