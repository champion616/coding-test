class Solution {
    public String solution(int age) {
        String answer = "";
        
        String ageStr = age + "";
        for(int i=0; i<ageStr.length(); i++){
            answer += (char)((int)ageStr.charAt(i) + 49);
        }
        
        return answer;
    }
}
